# Core ML stack (Kaggle-compatible)
tensorflow>=2.12.0
keras>=3.0.0
numpy>=1.23.0
scipy>=1.10.0

# Data / serialization
h5py>=3.8.0

# Visualisation
matplotlib>=3.7.0

# Config
pyyaml>=6.0

# Utilities
scikit-learn>=1.2.0     # optional: for additional metrics
requests>=2.28.0        # for download_weights.py

# Reproducibility note:
# Pin exact versions after first successful Kaggle run by running:
#   pip freeze > requirements_frozen.txt
# and committing requirements_frozen.txt as the canonical reproducibility record.
